// import IPFS from 'ipfs'
import Vue from 'vue'

const plugin = {
  install (Vue, opts = {}) {
    // IPFS.create(opts).then(function (value) {
    //   Vue.prototype.$ipfs = value
    // })
  }
}

Vue.use(plugin)
